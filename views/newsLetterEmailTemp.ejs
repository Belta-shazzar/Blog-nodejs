<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weekly Newsletter</title>
    <style>
      * {
        box-sizing: border-box;
        padding: 0;
        margin: 0;
      }

      :root {
        --dark-color: #443a2a;
      }

      body {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
          sans-serif;
        padding: 15px;
        color: var(--dark-color);
      }

      li {
        list-style: none;
      }

      .header {
        color: var(--dark-color);
        padding: 5px 0 20px;
        letter-spacing: 0.5px;
        font-size: medium;
        text-align: center;
        box-shadow: inset 0 -8px 15px -20px;
      }

      .list-items {
        display: grid;
        line-height: 25px;
        grid-template-columns: repeat(2, 1fr);
        margin-top: 16px;
        border-bottom: 1px #eee solid;
        box-shadow: inset 0 -8px 15px -20px #000;
      }

      /* .list-content span {
  display: flex;
  justify-content: space-between;
  max-width: 23%;
} */

      .move-btn {
        display: flex;
        justify-content: end;
        align-items: center;
      }

      li .btn {
        background: var(--dark-color);
        color: #fff;
        border-radius: 3px;
        text-align: end;
        text-decoration: none;
        padding: 5px;
      }

      .author {
        font-weight: bold;
      }

      .author::before {
        content: "-- ";
      }

      @media (max-width: 400px) {
        .header {
          font-size: small;
        }

        .list-items {
          display: inline;
          margin-top: 16px;
          padding-bottom: 5px;
        }

        .title {
          font-size: larger;
          font-weight: 800;
        }

        .author {
          text-align: end;
          margin: 8px;
          font-weight: bold;
        }

        .move-btn {
          display: block;
        }

        li .btn {
          background: none;
          color: #4267b2;
          letter-spacing: 0.5px;
          font-size: large;
          text-align: center;
          display: block;
        }

        .date {
          text-align: end;
          margin-right: 8px;
        }

        .list-content span {
          display: inline;
        }
      }
    </style>
    <script
      src="https://kit.fontawesome.com/793ae62f6d.js"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <header class="header">
      <h1>Uploaded articles from this past week</h1>
    </header>

    <ul>
      <% if (articles.length > 0) { %> <% for (let i = 0; i < articles.length;
      i++) { %>
      <li class="list-items">
        <div class="list-content">
          <h2 class="title"><%= articles[i].title %></h2>
          <p class="author"><%= articles[i].authorName %></p>
          <span>
            <p class="date"><%= articles[i].created %></p>
            <p class="likes">
              <i class="fas fa-thin fa-heart"></i> <%= articles[i].likes %> likes
            </p>
          </span>
        </div>
        <span class="move-btn">
          <a href=<%= articles[i].url %> class="btn">read me</a>
        </span>
      </li>
      <% } %> <% } else { %>
      <p></p>
      <% } %>
    </ul>
  </body>
</html>